{
  "name": "account-manager",
  "version": "1.0.0",
  "description": "Simple categorized account & credential manager with masked previews",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "test": "node --test tests/**/*.test.js",
    "vercel-build": "mkdir -p public"
  },
  "engines": {
    "node": ">=22.x"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^5.2.1"
  }
}
